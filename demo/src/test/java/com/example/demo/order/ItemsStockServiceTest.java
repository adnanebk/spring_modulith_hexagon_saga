package com.example.demo.order;

import org.springframework.boot.test.autoconfigure.orm.jpa.DataJpaTest;


@DataJpaTest
class ItemsStockServiceTest {

/*
    private ItemsStockService itemsStockService;
    @Autowired
     private ItemSpringRepo itemRepoPort;

    @BeforeEach
      void setup(){
         itemsStockService = new ItemsStockService(new ItemRepo(itemRepoPort,new ItemMapper()));
    }



    @Test
    void shouldCreateItem() {
   var item = new OrderItem("pizza",7.0,1);

   OrderItem createdItem = itemsStockService.createItem(item);

   Assertions.assertNotNull(createdItem);
   Assertions.assertNotNull(createdItem.getId());


    }*/

}